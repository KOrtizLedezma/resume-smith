import subprocess

subprocess.run(["python3", "scripts/job_description.py"])

subprocess.run(["python3", "scripts/tech_stack.py"])

subprocess.run(["python3", "scripts/projects.py"])

subprocess.run(["python3", "scripts/cover_letter.py"])

subprocess.run(["python3", "scripts/generate_resume_and_cover_letter.py"])
